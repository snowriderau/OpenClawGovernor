# Auth by Role, Not by Restriction

Agents get the tools they need. Workspace files define the role. Trust the model to follow it.

- Don't strip tools to force behavior — write clear role instructions instead
- Don't add approval gates — smart routing by identity, not restriction
- If an agent does the wrong thing, fix the instructions, not the permissions
